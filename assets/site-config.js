/**
 * Site configuration — edit IDs here after you create accounts.
 * Leave blank to skip loading that service (site works fine without them).
 */
window.ATLAS_SITE = {
  /** Google Analytics 4 measurement ID (Admin → Data streams → Measurement ID). Example: G-XXXXXXXXXX */
  gaMeasurementId: '',

  /** Microsoft Clarity project ID (Settings → Overview). Free session replay + heatmaps. */
  clarityProjectId: '',

  siteUrl: 'https://atlassolutions.tech',
};
